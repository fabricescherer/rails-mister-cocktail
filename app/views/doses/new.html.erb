<h1>Add New Dose</h1>
<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :amount %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit "Add a dose", class: "btn btn-primary" %>
<% end %>
